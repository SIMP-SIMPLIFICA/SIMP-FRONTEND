# Block all third-party lifecycle scripts (postinstall, install, preinstall) to
# prevent supply-chain attacks. This repo has no native-binary build steps, so
# no manual workaround is needed — npm install is safe as-is.
ignore-scripts=true
